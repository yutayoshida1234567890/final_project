<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All preferences
    </h1>

    <a href="/preferences/new" class="btn btn-block btn-outline-secondary">
      Add a new preference
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          Cuisine
        </th>

        <th>
          Preference scale
        </th>

        <th>
          Dietary restriction
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @preferences.each do |preference| %>
      <tr>
        <!-- Display preference.id start -->
        <td>
          <%= preference.id %>
        </td>
        <!-- Display preference.id end -->

        <!-- Display preference.user_id start -->
        <td>
          <% if preference.user.present? %>
            <a href="/users/<%= preference.user.id %>">
              <%= preference.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display preference.user_id end -->

        <!-- Display preference.cuisine start -->
        <td>
          <%= preference.cuisine %>
        </td>
        <!-- Display preference.cuisine end -->

        <!-- Display preference.preference_scale start -->
        <td>
          <%= preference.preference_scale %>
        </td>
        <!-- Display preference.preference_scale end -->

        <!-- Display preference.dietary_restriction start -->
        <td>
          <%= preference.dietary_restriction %>
        </td>
        <!-- Display preference.dietary_restriction end -->

        <!-- Display preference.created_at start -->
        <td>
          <%= time_ago_in_words(preference.created_at) %> ago
        </td>
        <!-- Display preference.created_at end -->

        <!-- Display preference.updated_at start -->
        <td>
          <%= time_ago_in_words(preference.updated_at) %> ago
        </td>
        <!-- Display preference.updated_at end -->

        <td>
          <a href="/preferences/<%= preference.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @preferences, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
